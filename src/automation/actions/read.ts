// Deprecated — the `read` action was generalized into `get`. This re-export
// exists only so any out-of-tree imports don't break; nothing in the registry
// references it.
export { getAction as readAction } from './get';
