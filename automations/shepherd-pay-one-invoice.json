{
  "name": "Shepherd: pay one invoice (XPath, multi-step)",
  "description": "End-to-end XPath example. Toggles Pay All Invoices off, fills the payment-amount input for a specific invoice number, then opens the Payment Type dropdown. All inside the closed shadow root.",
  "tag": "action",
  "variables": {
    "invoice": "#48675",
    "amount": "22"
  },
  "steps": [
    {
      "action": "fill",
      "xpath": "//tr[.//a[normalize-space(text())='{{invoice}}']]//input[@data-test-id='invoice-paymentTable-input']",
      "value": "{{amount}}",
      "pierceClosed": true
    },
    { "action": "wait", "ms": 200 },
    {
      "action": "click",
      "xpath": "//div[contains(@class, 'react-select-payment__dropdown-indicator')]",
      "pierceClosed": true
    }
  ]
}
