{
  "name": "Get first big number on page (regex)",
  "description": "Reads body innerText and extracts the first number with 3+ digits via regex.",
  "tag": "get",
  "steps": [
    {
      "action": "get",
      "xpath": "//body",
      "property": "innerText",
      "regex": "(\\d{3,}(?:[,.]\\d+)*)",
      "saveAs": "firstNumber"
    }
  ]
}
