{
  "name": "Example: read inside Shadow DOM",
  "description": "XPath via DOM.performSearch traverses every shadow root automatically — including closed ones when pierceClosed:true is set. This template targets a fictional <my-component> with an input; adapt to whatever component you're inspecting.",
  "tag": "get",
  "steps": [
    {
      "action": "get",
      "xpath": "//my-component//input[contains(@class, 'email')]",
      "saveAs": "emailFromShadow",
      "pierceClosed": true
    }
  ]
}
